{
  "buildCommand": "pnpm --filter @nexor/api build && rm -rf .next && cp -R apps/api/.next .next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "crons": [],
  "git": {
    "deploymentEnabled": true
  }
}
