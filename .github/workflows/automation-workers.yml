name: Nexor realtime automation workers

on:
  schedule:
    - cron: '*/5 * * * *'
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write

concurrency:
  group: nexor-realtime-workers
  cancel-in-progress: false

jobs:
  workers:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Allow production deployment to settle after a main push
        if: ${{ github.event_name == 'push' }}
        run: sleep 90

      - name: Acquire GitHub OIDC machine token
        env:
          ACTIONS_ID_TOKEN_REQUEST_URL: ${{ env.ACTIONS_ID_TOKEN_REQUEST_URL }}
          ACTIONS_ID_TOKEN_REQUEST_TOKEN: ${{ env.ACTIONS_ID_TOKEN_REQUEST_TOKEN }}
        run: |
          set -euo pipefail
          test -n "${ACTIONS_ID_TOKEN_REQUEST_URL:-}" || { echo 'GitHub Actions OIDC is unavailable'; exit 1; }
          test -n "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" || { echo 'GitHub Actions OIDC request token is unavailable'; exit 1; }
          oidc_url="${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=nexoraios-production"
          token="$(curl --silent --show-error --fail-with-body --max-time 30 \
            -H "Authorization: bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" \
            "$oidc_url" | jq -r '.value')"
          test -n "$token" && test "$token" != "null" || { echo 'Failed to obtain GitHub OIDC token'; exit 1; }
          echo "::add-mask::$token"
          echo "GITHUB_OIDC_TOKEN=$token" >> "$GITHUB_ENV"

      - name: Run automatic campaign discovery
        id: discovery
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 300 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" "$NEXOR_BASE_URL/api/campaigns/auto/run")"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "Campaign discovery returned HTTP $http_code"; exit 1; }

      - name: Run durable Nexor automation scheduler
        id: scheduler
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 120 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" -H "Content-Type: application/json" -X POST "$NEXOR_BASE_URL/api/automations/run")"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "Automation scheduler returned HTTP $http_code"; exit 1; }

      - name: Generate WhatsApp batch
        id: whatsapp_generate
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 120 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" -H "Content-Type: application/json" -X POST "$NEXOR_BASE_URL/api/whatsapp/automation" --data '{"action":"generate","limit":20}')"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "WhatsApp batch generation returned HTTP $http_code"; exit 1; }

      - name: Auto-approve eligible WhatsApp batch
        id: whatsapp_approve
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 120 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" -H "Content-Type: application/json" -X POST "$NEXOR_BASE_URL/api/whatsapp/automation/auto-approve")"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "WhatsApp auto-approval returned HTTP $http_code"; exit 1; }

      - name: Send approved WhatsApp batch with safe pacing
        id: whatsapp_send
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 240 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" -H "Content-Type: application/json" -X POST "$NEXOR_BASE_URL/api/whatsapp/automation" --data '{"action":"run_due","limit":20}')"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "WhatsApp batch sender returned HTTP $http_code"; exit 1; }

      - name: Run due follow-ups
        id: followups
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 120 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" "$NEXOR_BASE_URL/api/cron/followups")"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "Follow-up worker returned HTTP $http_code"; exit 1; }

      - name: Run due outreach
        id: outreach
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 120 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" "$NEXOR_BASE_URL/api/cron/outreach")"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "Outreach worker returned HTTP $http_code"; exit 1; }

      - name: Run due social publishing
        id: social
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 120 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" "$NEXOR_BASE_URL/api/cron/social-publish")"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "Social publishing worker returned HTTP $http_code"; exit 1; }

      - name: Send 3-hour growth and milestone report when due
        id: reporting
        continue-on-error: true
        env:
          NEXOR_BASE_URL: https://nexoraios-main-1.vercel.app
        run: |
          set -euo pipefail
          body_file="$(mktemp)"
          http_code="$(curl --silent --show-error --retry 2 --retry-delay 2 --retry-all-errors --max-time 120 -o "$body_file" -w '%{http_code}' -H "Authorization: Bearer $GITHUB_OIDC_TOKEN" "$NEXOR_BASE_URL/api/cron/reports/growth")"
          cat "$body_file"; echo; rm -f "$body_file"
          [[ "$http_code" =~ ^2[0-9][0-9]$ ]] || { echo "Growth reporting worker returned HTTP $http_code"; exit 1; }

      - name: Automation health summary
        if: ${{ always() }}
        run: |
          echo "discovery=${{ steps.discovery.outcome }}"
          echo "scheduler=${{ steps.scheduler.outcome }}"
          echo "whatsapp_generate=${{ steps.whatsapp_generate.outcome }}"
          echo "whatsapp_approve=${{ steps.whatsapp_approve.outcome }}"
          echo "whatsapp_send=${{ steps.whatsapp_send.outcome }}"
          echo "followups=${{ steps.followups.outcome }}"
          echo "outreach=${{ steps.outreach.outcome }}"
          echo "social=${{ steps.social.outcome }}"
          echo "reporting=${{ steps.reporting.outcome }}"
          if [[ "${{ steps.discovery.outcome }}" != "success" || "${{ steps.scheduler.outcome }}" != "success" || "${{ steps.whatsapp_generate.outcome }}" != "success" || "${{ steps.whatsapp_approve.outcome }}" != "success" || "${{ steps.whatsapp_send.outcome }}" != "success" || "${{ steps.followups.outcome }}" != "success" || "${{ steps.outreach.outcome }}" != "success" || "${{ steps.social.outcome }}" != "success" || "${{ steps.reporting.outcome }}" != "success" ]]; then
            echo "One or more Nexor automation workers failed. Inspect the individual step output above."
            exit 1
          fi
