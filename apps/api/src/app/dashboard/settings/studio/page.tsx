'use client';
import{useEffect,useRef,useState}from'react';
import Link from'next/link';
import DashboardLayout from'@/components/dashboard/DashboardLayout';
const automation=[['master_autopilot','Master Autopilot'],['outbound_enabled','Outbound communications'],['campaign_discovery','Automatic lead discovery'],['scheduler','Durable automation scheduler'],['job_autopilot','AI job autopilot'],['autopilot','Daily business autopilot'],['whatsapp_generation','WhatsApp generation'],['whatsapp_sending','WhatsApp sending'],['followups','Automatic follow-ups'],['outreach','Automatic outreach queue'],['social_publishing','Social publishing'],['daily_reports','Daily reports'],['growth_reports','Growth reports']];
const defaults={theme:'Executive Pearl',accent:'Indigo',density:'Comfortable',font_scale:'Default',founder_name:'Dev',founder_role:'Founder · Nexor Media',avatar_url:'/founder-avatar.svg'};
export default function SettingsStudio(){const[values,setValues]=useState<any>(defaults);const[server,setServer]=useState<Record<string,boolean>>({});const[status,setStatus]=useState('');const[ref]=[useRef<HTMLInputElement>(null)];
useEffect(()=>{Promise.all([fetch('/api/founder/profile',{cache:'no-store'}),fetch('/api/automations/settings',{cache:'no-store'})]).then(async([p,a])=>{const x=await p.json().catch(()=>({})),y=await a.json().catch(()=>({}));if(x?.success)setValues({...defaults,...x.profile});if(Array.isArray(y?.settings))setServer(Object.fromEntries(y.settings.map((s:any)=>[s.key,Boolean(s.enabled)])))}).catch(()=>setStatus('Could not load founder settings'))},[]);
const update=(k:string,v:any)=>setValues((x:any)=>({...x,[k]:v}));
const save=async()=>{setStatus('Saving…');try{const p=await fetch('/api/founder/profile',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({profile:values})});if(!p.ok)throw new Error('Could not save founder profile');for(const[key,label]of automation){const r=await fetch('/api/automations/settings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({key,enabled:Boolean(server[key])})});if(!r.ok)throw new Error(`Could not save ${label}`)}localStorage.setItem('nexor-settings-v2',JSON.stringify(values));window.dispatchEvent(new Event('nexor-settings-updated'));setStatus('Saved to production database · identity + appearance + automation')}catch(e){setStatus(e instanceof Error?e.message:String(e))}};
const upload=(f:File)=>{if(!f.type.startsWith('image/'))return setStatus('Choose an image file');if(f.size>8_000_000)return setStatus('Photo must be under 8 MB');const img=new Image();const r=new FileReader();r.onload=()=>{img.onload=()=>{const max=640,scale=Math.min(1,max/Math.max(img.width,img.height));const c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.width*scale));c.height=Math.max(1,Math.round(img.height*scale));const ctx=c.getContext('2d');if(!ctx)return setStatus('Could not process photo');ctx.drawImage(img,0,0,c.width,c.height);const data=c.toDataURL('image/jpeg',.82);if(data.length>790_000)return setStatus('Photo is still too large; choose a simpler image');update('avatar_url',data);setStatus('Photo ready · press SAVE ALL to persist it')};img.src=String(r.result)};r.readAsDataURL(f)};
return <DashboardLayout><main className="mx-auto w-full max-w-6xl space-y-4 sm:space-y-5"><section className="nexor-panel p-5 sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-5"><button onClick={()=>ref.current?.click()} className="relative h-28 w-28 shrink-0 overflow-hidden rounded-3xl border border-[var(--accent)]/30 bg-[var(--surface-2)] shadow-xl"><img src={values.avatar_url} alt="Founder" className="h-full w-full object-cover" onError={e=>{e.currentTarget.src='/founder-avatar.svg'}}/><span className="absolute inset-x-2 bottom-2 rounded-lg bg-black/70 py-1.5 text-center text-[7px] font-bold text-white">CHANGE PHOTO</span></button><input ref={ref} type="file" accept="image/*" hidden onChange={e=>{const f=e.target.files?.[0];if(f)upload(f)}}/><div><div className="font-mono text-[7px] tracking-[.2em] text-[var(--accent)]">NEXOR MEDIA · FOUNDER SETTINGS</div><h1 className="mt-1 text-2xl font-semibold">Your personal command identity</h1><p className="mt-1.5 max-w-2xl text-[9px] leading-5 text-[var(--text-secondary)]">Your DP is resized and persisted in the production database, then reused in the top-right profile, founder panel and navigation after save.</p></div></div><div className="rounded-xl bg-[var(--surface-2)] px-4 py-3 text-[8px] text-[var(--text-muted)]">{status||'All changes are saved server-side when you press SAVE ALL.'}</div></div></section><section className="grid gap-4 lg:grid-cols-2"><div className="nexor-panel p-5"><div className="text-[12px] font-semibold">Appearance</div><div className="mt-4 grid gap-3"><label className="text-[8px] font-mono text-[var(--text-muted)]">THEME<select value={values.theme} onChange={e=>update('theme',e.target.value)} className="mt-1 h-10 w-full rounded-lg border border-[var(--border)] bg-[var(--surface-2)] px-3 text-[10px]"><option>Executive Pearl</option><option>Obsidian</option><option>Light</option><option>Gold Executive</option></select></label><label className="text-[8px] font-mono text-[var(--text-muted)]">ACCENT<select value={values.accent} onChange={e=>update('accent',e.target.value)} className="mt-1 h-10 w-full rounded-lg border border-[var(--border)] bg-[var(--surface-2)] px-3 text-[10px]">{['Indigo','Violet','Cyan','Emerald','Gold','Monochrome'].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-[8px] font-mono text-[var(--text-muted)]">FONT SIZE<select value={values.font_scale} onChange={e=>update('font_scale',e.target.value)} className="mt-1 h-10 w-full rounded-lg border border-[var(--border)] bg-[var(--surface-2)] px-3 text-[10px]"><option>Small</option><option>Default</option><option>Large</option><option>XL</option></select></label><label className="text-[8px] font-mono text-[var(--text-muted)]">DENSITY<select value={values.density} onChange={e=>update('density',e.target.value)} className="mt-1 h-10 w-full rounded-lg border border-[var(--border)] bg-[var(--surface-2)] px-3 text-[10px]"><option>Comfortable</option><option>Compact</option><option>Dense</option><option>Terminal</option></select></label></div></div><div className="nexor-panel overflow-hidden"><div className="p-5"><div className="text-[12px] font-semibold">Autopilot & outbound</div><div className="mt-1 text-[8px] text-[var(--text-muted)]">These switches are stored in the production database and consumed by automation guards.</div></div><div className="divide-y divide-[var(--border)]">{automation.map(([key,label])=><div key={key} className="flex items-center gap-4 px-5 py-3.5"><div className="min-w-0 flex-1"><div className="text-[10px] font-semibold">{label}</div><div className="mt-0.5 font-mono text-[7px] text-[var(--text-muted)]">{server[key]?'ENABLED':'PAUSED'}</div></div><button onClick={()=>setServer(x=>({...x,[key]:!x[key]}))} className={`relative h-7 w-12 rounded-full ${server[key]?'bg-[var(--primary)]':'bg-[var(--surface-3)]'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white transition ${server[key]?'left-6':'left-1'}`}/></button></div>)}</div></div></section><section className="nexor-panel p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="text-[12px] font-semibold">Founder identity</div><div className="mt-1 text-[8px] text-[var(--text-muted)]">Name and role appear throughout the founder UI.</div></div><div className="flex flex-wrap gap-2"><input value={values.founder_name} onChange={e=>update('founder_name',e.target.value)} className="h-10 w-36 rounded-lg border border-[var(--border)] bg-[var(--surface-2)] px-3 text-[9px]"/><input value={values.founder_role} onChange={e=>update('founder_role',e.target.value)} className="h-10 w-52 rounded-lg border border-[var(--border)] bg-[var(--surface-2)] px-3 text-[9px]"/><button onClick={save} className="rounded-lg bg-[var(--primary)] px-5 py-2.5 text-[9px] font-bold text-white">SAVE ALL</button></div></div><div className="mt-4 flex flex-wrap gap-3"><Link href="/dashboard/settings/admin" className="rounded-lg border border-[var(--border)] px-4 py-2.5 text-[8px] font-bold">OPEN FOUNDER ADMIN →</Link><Link href="/dashboard/settings/automation" className="rounded-lg border border-[var(--border)] px-4 py-2.5 text-[8px] font-bold">AUTOMATION CONTROL →</Link></div></section></main></DashboardLayout>}
