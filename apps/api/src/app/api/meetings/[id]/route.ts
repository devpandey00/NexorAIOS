import { NextRequest, NextResponse } from 'next/server';
import { getDatabaseClients, MeetingStatus } from '@nexor/database';
const db = () => getDatabaseClients().write;
export async function GET(_: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const meeting = await db().meeting.findUnique({ where: { id }, include: { lead: true, opportunity: true } }); if (!meeting) return NextResponse.json({ success:false,error:'Meeting not found' },{status:404}); return NextResponse.json({success:true,meeting}); } catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:String(e)},{status:500});}}
export async function PATCH(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const { id }=await params; const body=await req.json(); const data:any={}; if(typeof body.title==='string') data.title=body.title.trim(); if(typeof body.scheduledAt==='string') data.scheduledAt=new Date(body.scheduledAt); if(typeof body.meetingUrl==='string') data.meetingUrl=body.meetingUrl; if(typeof body.notes==='string') data.notes=body.notes; if(typeof body.status==='string' && Object.values(MeetingStatus).includes(body.status)) data.status=body.status; const meeting=await db().meeting.update({where:{id},data}); return NextResponse.json({success:true,meeting}); } catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:String(e)},{status:400});}}
