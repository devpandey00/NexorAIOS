'use client';
import Link from 'next/link';import{usePathname}from'next/navigation';
const items=[['Home','/dashboard','⌂'],['Leads','/dashboard/tools/lead-inbox','◎'],['Outreach','/dashboard/tools/whatsapp-drafts','↗'],['Inbox','/dashboard/tools/whatsapp-inbox','□'],['Settings','/dashboard/settings/studio','⚙']] as const;
export default function MobileNav(){const pathname=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-50 border-t border-[var(--border)] bg-[var(--surface)]/96 px-2 pt-1.5 backdrop-blur-xl lg:hidden" style={{paddingBottom:'calc(env(safe-area-inset-bottom) + 6px)'}}><div className="mx-auto grid max-w-lg grid-cols-5 gap-1">{items.map(([label,href,icon])=>{const active=pathname===href||(href!=='/dashboard'&&pathname.startsWith(href));return <Link key={label} href={href} className={active?'flex min-h-12 flex-col items-center justify-center rounded-[10px] bg-[var(--primary-soft)] text-[var(--primary)]':'flex min-h-12 flex-col items-center justify-center rounded-[10px] text-[var(--text-muted)]'}><span className="text-[15px] leading-none">{icon}</span><span className="mt-1 text-[9px] font-semibold">{label}</span></Link>})}</div></nav>}
