# NexorAIOS API

This is the NexorAIOS Next.js application. Vercel deployment uses the `@nexor/api` workspace build and exposes its Next.js build output at the repository root for the current monorepo deployment configuration.
